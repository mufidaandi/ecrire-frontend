import TextEditor from "../components/TextEditor";

const AddEntry = () => {
  return (
    <div className="add-entry bg-white text-black min-h-full w-full p-10">
      <TextEditor />
    </div>
  );
};

export default AddEntry;
